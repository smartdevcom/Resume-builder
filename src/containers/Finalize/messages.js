/*
 * Finalize Messages
 *
 * This contains all the text for the Finalize container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Finalize';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Finalize container!',
  },
});
