import { makeStyles } from '@material-ui/core';

export const educationStyles = makeStyles(theme => ({
  main: {
    backgroundColor: '#f7f7f7' || theme.palette.grey,
    minHeight: '100vh',
  },
}));
