/*
 * Summary Messages
 *
 * This contains all the text for the Summary container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Summary';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Summary container!',
  },
});
