/*
 * WorkHistory Messages
 *
 * This contains all the text for the WorkHistory container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WorkHistory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WorkHistory container!',
  },
});
