/*
 * SkillsChild Messages
 *
 * This contains all the text for the SkillsChild component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SkillsChild';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SkillsChild component!',
  },
});
