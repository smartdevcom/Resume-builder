/*
 * EducationChild Messages
 *
 * This contains all the text for the EducationChild component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EducationChild';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EducationChild component!',
  },
});
